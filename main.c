#include "main.h"

/**
 * main - code entry point
 * @ac: argument count
 * @av: argument variables
 *
 * Return: always 0 success
 */


int main(int ac, char **av)
{
	int i = ac;

	_getcommand(av);

	i = 0;
	return (i);
}
